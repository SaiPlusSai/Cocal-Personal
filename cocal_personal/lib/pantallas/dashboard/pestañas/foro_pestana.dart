import 'package:flutter/material.dart';

class ForoPestana extends StatelessWidget {
  const ForoPestana({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('💬 Foro (Próximamente)'));
  }
}
