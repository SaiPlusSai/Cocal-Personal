import 'package:flutter/material.dart';

class CalendarioPestana extends StatelessWidget {
  const CalendarioPestana({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('📅 Calendario (En desarrollo)'));
  }
}
